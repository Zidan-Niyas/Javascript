// Ternary Operators (alternative to if-else statements ---> important for REACT)
let myName = age > 10 ? "Zidan" : "Jack";  
const Component = () => {
    return age > 10 ? <div>Zidan</div> : <div>Jack</div>;
}
